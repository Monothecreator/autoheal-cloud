apiVersion: v1
kind: Service
metadata:
  name: autoheal-cloud
  namespace: autoheal
  labels:
    app: autoheal-cloud
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: autoheal-cloud
